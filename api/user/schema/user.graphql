const { gql } = require('apollo-server')

const userSchema = gql `
  type Role {
    id: ID!
    type: String!
  }

  type User {
    name: String!
    active: Boolean!
    email: String
    role: Role!
  }

  type Query {
    users: [User]
    user(id: ID!): User
  }
`

module.exports = userSchema